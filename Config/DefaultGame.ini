[/Script/EngineSettings.GeneralProjectSettings]
ProjectID=356E60E14DBA67ACCEFF4184F61ACF4D
ProjectName=Sculpturillo
ProjectVersion=0.9.2

[ConsoleVariables]
CommonUI.CheckKeyboardFocusAndParentage=1
CommonUI.DisallowUserFocusedWidgetForPendingFocusRecipient=1
CommonUI.FallbackToDesiredOnAutoRestoreFailure=1

[/Script/UnrealEd.ProjectPackagingSettings]
+MapsToCook=(FilePath="/Game/Template/levels/BootLVl")
+MapsToCook=(FilePath="/Game/Template/levels/MainMenu")
+MapsToCook=(FilePath="/Game/Template/levels/Lvl-01")
+MapsToCook=(FilePath="/Game/Template/levels/TransitionMap")
; Plantilla de los mapas de PROPS (se abre por nombre en runtime → hay que cocinarla explícito, si no
; el build no la incluye y crear/jugar un mapa de props cae a BootLVl).
+MapsToCook=(FilePath="/MapKit/Mapa_Plantilla")
+DirectoriesToAlwaysCook=(Path="/MapKit")
; BP_MapAuthorGameMode se referencia SOLO por texto (?game= en la URL al entrar a autoría), así que el
; cooker no lo incluye solo → sin esto el override falla y el mapa arranca en su GameMode de jugar.
; Cocinar toda la carpeta Character asegura el author GM (y sus dependencias).
+DirectoriesToAlwaysCook=(Path="/Game/Template/Character")
; Los textos de la UI y el banco de palabras son CSV SUELTOS (Content/Localization/*.csv), no
; assets: hay que empaquetarlos a mano o el juego sale sin traducciones y sin palabras.
; UFS = van adentro del pak y FFileHelper los lee igual que en el editor.
; (Antes esto forzaba el cook de /Game/Template/Data por el DT_WordBank; esos DataTables ya no
;  existen — los reemplazaron los CSV, que son los que permiten sumar idiomas sin recompilar.)
; OJO: la ruta es relativa a Content/ — poner "Content/Localization" hace que busque
; Content/Content/Localization y los CSV quedan AFUERA del pak sin que el build falle.
+DirectoriesToAlwaysStageAsUFS=(Path="Localization")
; PNG del cursor por defecto (hardware cursor, ver [/Script/Engine.UserInterfaceSettings] en
; DefaultEngine.ini). Es un PNG suelto (no uasset) → si no se stagea, el build sale sin cursor custom.
+DirectoriesToAlwaysStageAsUFS=(Path="UI/Cursors")
; PNG por defecto de miniatura del Workshop (se lee el PNG suelto, no el uasset). Sin esto, en el
; build empaquetado no estaría y el fallback caería al PNG sólido de relleno.
+DirectoriesToAlwaysStageAsUFS=(Path="UI/Workshop")
; Bancos de palabras OFICIALES del juego (Content/WordPacks/<nombre>/words.csv + pack.txt + preview.png).
; Son archivos sueltos (CSV/PNG/TXT), así que hay que stagearlos o no aparecen en la build.
+DirectoriesToAlwaysStageAsUFS=(Path="WordPacks")
bUseZenStore=False
bUseIoStore=False
